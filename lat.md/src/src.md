# src

This index maps source files that have dedicated `lat.md` pages so file-level concepts can be browsed alongside the thematic architecture docs.

- [[config.rs]] — Loads `config.toml` into typed structs, exposes the global singleton, and provides radiator/spec helper calculations.
